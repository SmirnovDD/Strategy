using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoundController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //public void VolumeChange()
    //{
    //    musicVolumeLerped = Mathf.Lerp(-80, 0, musicVolume.value);
    //    soundsVolumeLerped = Mathf.Lerp(-80, 0, soundsVolume.value);

    //    if (musicVolume.value == 0)
    //        musicVolumeImage.sprite = soundOffSprite;
    //    else
    //        musicVolumeImage.sprite = soundOnSprite;

    //    if (soundsVolume.value == 0)
    //        soundsVolumeImage.sprite = soundOffSprite;
    //    else
    //        soundsVolumeImage.sprite = soundOnSprite;

    //    mixer.SetFloat("MusicVolume", musicVolumeLerped);
    //    mixer.SetFloat("SoundsVolume", soundsVolumeLerped);
    //}
}
